<%= form_for :categories, {url: {controller: 'categories', action: 'create'}, remote: true} do |f| %>
	<%= f.text_field :category_name, placeholder: "Add New category" %>
	<%= f.submit "Add", method: 'post' %>
<% end -%>
